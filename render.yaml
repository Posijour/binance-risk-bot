services:
  - type: web
    name: coinglass-risk-bot
    env: python
    buildCommand: pip install --no-cache-dir -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: COINGLASS_API_KEY
        sync: false
